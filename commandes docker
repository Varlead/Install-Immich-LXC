docker compose up -d --force-recreate

  - Logs pour Immich
docker logs immich_server -f


  - Upgrade and restart Immich
docker compose pull && docker compose up -d


  - Clean up unused Docker images
docker image prune


  - Passer en mode terminal dans un container
docker exec -it <id du conteneur> bash

  - Liste des containter
docker ps
